services:
  - type: web
    name: springrestapi
    runtime: java
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/springrestapi-0.0.1-SNAPSHOT.jar
    plan: free
    envVars:
      - key: PORT
        value: 8080